body {
	font-family: 'courier new', monospace;
	color: $c-text;
	line-height: 1.4;
}

h1 {
	margin-top: 0;
}

::selection {
	color: $c-highlight;
	background-color: $c-highlight-bg;
}

a,
a:visited {
	color: inherit;
}
